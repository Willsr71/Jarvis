subprojects {
    apply plugin: "java"
    apply plugin: "idea"

    dependencies {
        compileOnly rootProject
    }

    build {
        dependsOn clean
        dependsOn jar

        jar.mustRunAfter clean
    }
}