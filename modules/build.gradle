subprojects {
    apply plugin: "java"
    apply plugin: "idea"

    dependencies {
        compile rootProject
    }

    build {
        dependsOn clean
        dependsOn jar

        jar.mustRunAfter clean
    }
}